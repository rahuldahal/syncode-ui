export const collabMessages = {
  invitation: (username: string, filename: string) =>
    `${username} is inviting you to collaborate on the file ${filename}. Check it out to start working together!`,
};
